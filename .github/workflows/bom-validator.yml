name: BOM Validator

on:
  - push
  - pull_request

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
      - run: go get neilpa.me/yajsv
      - run: /home/runner/go/bin/yajsv -s cyclonedx-schema-1_2.json bom.json
